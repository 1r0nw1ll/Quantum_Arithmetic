# Task for Codex Agent: Fix SPECLOCK.manifest Test Failure

**Issue:**
The `test_speclock_manifest_matches_rules_hash` in `qa_core/tests/test_sanity.py` is failing.
The test expects the `SPECLOCK.manifest` to contain absolute paths for `QA_RULES.yaml`, but the manifest currently uses relative paths.

**Error Message:**
```
AssertionError: Could not parse expected hash from manifest
```

**Details:**
-   **Test File:** `qa_lab/qa_core/tests/test_sanity.py`
-   **Manifest File:** `qa_lab/context/SPECLOCK.manifest`
-   **Rules File:** `qa_lab/context/QA_RULES.yaml`

The test's parsing logic (`parts[1] == str(rules)`) is comparing the manifest's relative path (`context/QA_RULES.yaml`) with the absolute path generated by `str(rules)` (`/home/player2/signal_experiments/qa_lab/context/QA_RULES.yaml`).

**Proposed Fix:**
Modify `qa_core/tests/test_sanity.py` to compare `parts[1]` with the *relative* path of `QA_RULES.yaml` from the `BASE` directory.

**Specific Change:**
In `qa_core/tests/test_sanity.py`, change the line:
```python
        if len(parts) >= 2 and parts[1] == str(rules):
```
to:
```python
        if len(parts) >= 2 and parts[1] == str(rules.relative_to(BASE)):
```

This will align the test's expectation with the manifest's content, allowing the test to pass and the `make meta` command to proceed.
